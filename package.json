{
  "name": "meetmind",
  "version": "1.0.0",
  "description": "AI meeting intelligence agent -- personalized action items for every attendee",
  "private": true,
  "scripts": {
    "deploy": "vercel --prod",
    "diagnostics": "node build/diagnostics.js",
    "start": "npx vercel dev --listen 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "groq-sdk": "^0.8.0",
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
